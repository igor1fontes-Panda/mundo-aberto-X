{
  "name": "mundo-aberto-x",
  "version": "7.0.0",
  "private": true,
  "type": "commonjs",
  "description": "Sociedade expansível de agentes AI em 3D top-down (Vite + React + Three.js): escola/academia, professores auto-aprendentes, código de conduta, mapa RPG expansível, fauna, touch, língua emergente Lume.",
  "scripts": {
    "dev": "npm run gen && vite --host 0.0.0.0 --port ${PORT:-3000} --strictPort",
    "build": "npm run gen && vite build",
    "gen": "node -e \"const fs=require('fs');fs.mkdirSync('public',{recursive:true});fs.copyFileSync('engine.js','public/engine.js');console.log('public/engine.js sincronizado com engine.js')\" && node scripts/gen-icons.js",
    "test": "node engine.test.js",
    "start": "npm run dev"
  },
  "dependencies": {
    "@vercel/speed-insights": "^2.0.0",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "recharts": "2.15.4",
    "three": "0.170.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
